/**
 * Position-joint subsystem package.
 *
 * <p>Contains:
 *
 * <ul>
 *   <li>{@link frc.robot.subsystems.position_joint.PositionJoint}: subsystem-level behavior,
 *       profiling, and tunable gains.</li>
 *   <li>{@link frc.robot.subsystems.position_joint.PositionJointIO}: hardware abstraction
 *       interface.</li>
 *   <li>Hardware-specific IO implementations for TalonFX, SparkMax, simulation, and replay.</li>
 *   <li>{@link frc.robot.subsystems.position_joint.PositionJointConstants}: shared gains and
 *       hardware configs.</li>
 * </ul>
 */
package frc.robot.subsystems.position_joint;
