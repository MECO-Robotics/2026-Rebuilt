Command Compositions
====================

Use commands to combine subsystem behaviors into operator controls, autos, and repeatable actions.

Start here
----------

- Configure controls in :doc:`configure/robotcontainer`
- Keep subsystem logic small and reusable
- Put multi-step robot behavior in commands instead of subsystems
